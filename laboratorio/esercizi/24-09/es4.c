#include <stdio.h>

/* Dichiara tre numeri interi x, y e z, inizializzati con 
valori 5, 8 e 11, e stampa il loro prodotto usando printf */

int main(){
    int x = 5, y = 8, z = 11;

    printf("Il prodotto di %d, %d e %d è %d\n", x, y, z, x * y * z);
    return 0;
}